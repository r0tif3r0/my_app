= form_for @user, html: { class: 'auth-form' } do |f|
  .form-header
    %h2= t('.create_account')

  - if @user.errors.any?
    .error-box
      %h3.error-title Ой! Возникли ошибки:
      %ul.error-list
        - @user.errors.full_messages.each do |msg|
          %li.error-item= msg

  .form-group
    = f.label t('.name'), class: 'form-label'
    = f.text_field :name, class: 'form-input', required: true

  .form-group
    = f.label t('.email'), class: 'form-label'
    = f.email_field :email, class: 'form-input', required: true

  .form-group
    = f.label t('.password'), class: 'form-label'
    = f.password_field :password, class: 'form-input', required: true

  .form-group
    = f.label t('.password_confirmation'), class: 'form-label'
    = f.password_field :password_confirmation, class: 'form-input', required: true

  .form-actions
    = f.submit t(".create_account_button"), class: "form-submit-btn"
